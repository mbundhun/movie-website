# This file is kept for reference but Render is not recommended
# Render requires payment after 1 month - use Railway or Fly.io instead
# See FREE_HOSTING_OPTIONS.md for free alternatives

# Railway deployment: Use Railway dashboard (no config file needed)
# Fly.io deployment: Use fly.toml (create with: fly launch)

# Note: Database is hosted on Supabase (free tier, no expiration)
# See DEPLOYMENT.md for deployment instructions using Railway/Fly.io

